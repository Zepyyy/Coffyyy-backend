info:
  name: Untitled
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/beans
  body:
    type: json
    data: |-
      {
       "name": "aled",
       "description": "ouais ouaiis ouais"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

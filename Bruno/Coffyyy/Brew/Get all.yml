info:
  name: Get all
  type: http
  seq: 1

http:
  method: GET
  url: "{{BaseUrl}}/api/brew"
  headers:
    - name: accept
      value: "*/*"
    - name: X-CSRF-TOKEN
      value: "{{CSRF-TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Get brew
  type: http
  seq: 2

http:
  method: GET
  url: "{{BaseUrl}}/api/brew/1"
  headers:
    - name: accept
      value: application/json
    - name: X-CSRF-TOKEN
      value: "{{CSRF-TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Delete brew
  type: http
  seq: 5

http:
  method: delete
  url: "{{BaseUrl}}/api/brew/1"
  headers:
    - name: accept
      value: "*/*"
    - name: X-CSRF-TOKEN
      value: "{{CSRF-TOKEN}}"
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5

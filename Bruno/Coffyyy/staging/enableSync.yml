info:
  name: enableSync
  type: http
  seq: 3

http:
  method: POST
  url: "{{BaseUrl}}/api/auth/sync/enable"
  headers:
    - name: X-CSRF-TOKEN
      value: "{{CSRF-TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Logout
  type: http
  seq: 5

http:
  method: POST
  url: "{{BaseUrl}}/api/auth/sync/logout"
  headers:
    - name: x-csrf-token
      value: "{{CSRF-TOKEN}}"
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

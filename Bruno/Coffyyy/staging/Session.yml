info:
  name: Session
  type: http
  seq: 3

http:
  method: GET
  url: "{{BaseUrl}}/api/auth/sync/session"
  headers:
    - name: X-CSRF-Token
      value: "{{CSRF-TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

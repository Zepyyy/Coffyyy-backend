info:
  name: Delete bean
  type: http
  seq: 5

http:
  method: delete
  url: "{{BaseUrl}}/api/bean/1"
  headers:
    - name: accept
      value: "*/*"
    - name: X-CSRF-TOKEN
      value: "{{CSRF-TOKEN}}"
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5

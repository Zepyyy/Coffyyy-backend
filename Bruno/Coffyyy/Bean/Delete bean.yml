info:
  name: Delete bean
  type: http
  seq: 2

http:
  method: delete
  url: http://localhost:3000/api/bean/2
  headers:
    - name: accept
      value: "*/*"
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Update bean
  type: http
  seq: 5

http:
  method: patch
  url: http://localhost:3000/api/bean/2
  headers:
    - name: accept
      value: "*/*"
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "brands": [
          "Terre de café", "TANAT"
        ]
      }
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5

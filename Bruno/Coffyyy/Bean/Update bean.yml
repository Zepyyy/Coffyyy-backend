info:
  name: Update bean
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{BaseUrl}}/api/bean/1"
  headers:
    - name: accept
      value: "*/*"
    - name: Content-Type
      value: application/json
    - name: X-CSRF-TOKEN
      value: "{{CSRF-TOKEN}}"
  body:
    type: json
    data: |-
      {
        "countries": [
          "FRANCE"
        ]
      }
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5

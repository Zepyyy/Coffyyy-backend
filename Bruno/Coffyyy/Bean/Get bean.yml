info:
  name: Get bean
  type: http
  seq: 2

http:
  method: get
  url: "{{BaseUrl}}/api/bean/3"
  headers:
    - name: accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Profile
  type: http
  seq: 2

http:
  method: get
  url: http://localhost:3000/api/auth/profile
  headers:
    - name: Authorization
      value: "Bearer "
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5

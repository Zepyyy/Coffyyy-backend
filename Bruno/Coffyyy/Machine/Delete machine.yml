info:
  name: Delete machine
  type: http
  seq: 5

http:
  method: delete
  url: http://localhost:3000/api/machine/3
  headers:
    - name: accept
      value: "*/*"
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5

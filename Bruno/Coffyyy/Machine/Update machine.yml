info:
  name: Update machine
  type: http
  seq: 4

http:
  method: patch
  url: http://localhost:3000/api/machine/1
  headers:
    - name: accept
      value: "*/*"
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "name": "skldjfb"
      }
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5

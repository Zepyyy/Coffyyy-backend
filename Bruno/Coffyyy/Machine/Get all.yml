info:
  name: Get all
  type: http
  seq: 1

http:
  method: get
  url: http://localhost:3000/api/machine
  headers:
    - name: accept
      value: "*/*"
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
